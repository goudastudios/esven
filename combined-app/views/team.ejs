<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Team - Esven</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/team.css" />
  </head>
  <body>
    <%- include('partials/header') %>
    <main class="team-page">
      <div class="modal fade" id="testModal" tabindex="-1">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title">Test Modal</h5>
              <button
                type="button"
                class="btn-close"
                data-bs-dismiss="modal"
              ></button>
            </div>
            <div class="modal-body">
              If you can see this, Bootstrap is working!
            </div>
          </div>
        </div>
      </div>
      <!-- Team Hero -->
      <section class="team-hero">
        <div class="container">
          <h1>The People Behind the Vision</h1>
          <div class="text-start">
            <p class="subtitle">
              Every great investment starts with understanding people. Our team
              brings together diverse backgrounds, shared values, and a deep
              commitment to building lasting relationships with founders. <br />
              <br />
              Esalen Ventures has a broad network of technical advisors who help
              us to evaluate potential investments. They are vital in the Due
              Diligence process and play an important role in our extended team.
              They have experience in a variety of different sectors and bring
              that expertise to bear on behalf of those we invest in.
            </p>
          </div>
        </div>
      </section>

      <!-- Team Principles -->
      <section class="team-principles">
        <div class="container">
          <div class="principles-grid">
            <div class="principle">
              <div class="principle-icon">⚡</div>
              <h3>Speed</h3>
              <p>
                Quick decisions, fast execution. We respect founders' time and
                move at startup speed.
              </p>
            </div>
            <div class="principle">
              <div class="principle-icon">🎯</div>
              <h3>Focus</h3>
              <p>
                Deep expertise in select sectors. We invest where we can add the
                most value.
              </p>
            </div>
            <div class="principle">
              <div class="principle-icon">🚀</div>
              <h3>Scale</h3>
              <p>
                Building companies that define categories and create lasting
                value at global scale.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Team Grid -->
      <section class="team-grid-section">
        <div class="container">
          <h2 class="section-title">Investment Partners</h2>
          <div class="team-grid">
            <% if (typeof teamMembers !== 'undefined' && teamMembers.length > 0)
            { %> <% teamMembers.forEach(member => { %> <%-
            include('partials/team-member-card', { member }) %> <% }) %> <% }
            else { %>
            <p>No team members found.</p>
            <% } %>
          </div>
        </div>
      </section>
    </main>
    <%- include('partials/footer') %>

    <!-- Team Member Modals -->
    <% teamMembers.forEach(member => { const modalId = 'teamModal' + (member.id
    || member.name.replace(/\s+/g, '')); %>
    <div class="modal fade" id="<%= modalId %>" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title"><%= member.name %></h5>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <div class="d-flex">
              <img
                src="<%= member.image %>"
                alt="<%= member.name %>"
                class="me-3"
                style="
                  width: 100px;
                  height: 100px;
                  object-fit: cover;
                  border-radius: 50%;
                "
              />
              <div>
                <h5 class="mb-1"><%= member.name %></h5>
                <p class="text-muted mb-2"><%= member.title %></p>
                <% if (member.metrics && member.metrics.length > 0) { %>
                <div class="d-flex gap-3 mb-3">
                  <% member.metrics.forEach(metric => { %>
                  <div class="metric">
                    <div class="metric-value"><%= metric.value %></div>
                    <div class="metric-label"><%= metric.label %></div>
                  </div>
                  <% }); %>
                </div>
                <% } %>
              </div>
            </div>
            <div class="mt-3"><%= member.bio %></div>
          </div>
        </div>
      </div>
    </div>
    <% }); %>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
